# Jekyll (legacy)
_site/
.sass-cache
.jekyll-metadata
Gemfile.lock
/assets/archive/decom/

# Hugo (new)
public/
resources/
.hugo_build.lock

# Legacy preview worktree (master branch checked out by `make serve-legacy`)
_legacy/

# Editor / IDE / AI tooling
.idea/
# .vscode/ is tracked - it's a single-dev repo, so shared editor config is
# a feature not a bug. If anything machine-specific surfaces (absolute paths,
# tokens, history/ folders), ignore that specific path, not the whole dir.
.cursor/
.specstory/

# OS
.DS_Store
Thumbs.db

# Personal / scratch (kept locally, not published)
.personal/

