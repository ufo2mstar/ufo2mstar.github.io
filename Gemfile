source 'https://rubygems.org'

gem 'wdm', '>= 0.1.0' if Gem.win_platform?

group :jekyll_plugins do
  gem 'jekyll-mermaid'

  gem 'jekyll-paginate'
  gem 'jemoji' # for github flavored emojis :) :+1:
  gem 'jekyll-relative-links'
#
  gem 'jekyll-gist'
  gem 'jekyll-seo-tag'
end

gem 'github-pages', group: :jekyll_plugins